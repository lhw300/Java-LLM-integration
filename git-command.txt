
git log --pretty=format:"%h %ad | %s" --date=format:"%Y-%m-%d %H:%M:%S"


改动一点代码（比如在 ChatExample2.java 加一行注释）。

运行 git status：你会看到文件变红了。

运行 git add .：文件变绿了（进了暂存区）。

运行 git commit -m "test: 练习一次提交"。

运行 git log --oneline：看看你的存档点是不是多了一个。


git checkout a1b2c3d  整个覆盖

如果你想把文件恢复到某个版本，现在的标准写法是：
git restore --source=a1b2c3d src/main/java/com/ChatExample2.java

git show a1b2c3d:src/main/java/com/ChatExample2.java > c:\tmp\ChatExample2_old.java

这条命令会把 a1b2c3d 版本的全部代码打成一个 .zip 包，扔进 c:\tmp。
git archive --format=zip -o c:\tmp\old_project.zip a1b2c3d


我建议你现在先玩一下分支，因为这个不需要联网，在你的 E 盘就能做。
你可以尝试以下操作：
输入：git checkout -b test-branch
随便改点代码并提交。
输入：git checkout main
去 Eclipse 看：你会发现刚才改的代码神奇地消失了！
输入：git checkout test-branch
代码又变回来了！

ssh-keygen -t ed25519 -C "lhw300@163.com"

获取公钥内容：
输入以下命令查看并复制出来的所有文字：
type %userprofile%\.ssh\id_ed25519.pub

ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKaiMeAFaIv+tmaW64IhwZb3LBoOlWanEBsMs8GBYJFW lhw300@163.com

绑定到 GitHub：
在 GitHub 网页右上角点击头像 -> Settings。
左侧菜单选 SSH and GPG keys。
点击 New SSH key，Title 随便起（如 "My-PC"），Key 粘贴你刚才复制的内容，保存。

第三步：把本地代码推上去
回到你的项目目录 E:\eit\openai，在命令行执行以下三行：
关联远程仓库（把 YOUR_URL 换成你刚才在 GitHub 页面看到的 .git 地址）：
git remote add origin git@github.com:lhw300/java-LLM-integration.git
验证关联 查看远端仓库名字
git remote -v

确保你在主分支：
git branch -M main
推送代码：


git push -u origin main
git push -u origin main 拆开看：
git push: 核心动作，把本地代码推送到远端。
origin: 远程仓库的“绰号”。它指向你 GitHub 仓库的 URL 地址。
main: 你本地的分支名称。
-u (或 --set-upstream): 这是关键。 它的意思是“建立关联”。它告诉 Git：以后我在这个分支下只输入 git push 或 git pull 时，默认就是和 origin 的 main 分支打交道。

如果别人改了云端，我是push不了了。
需要做fetch
git fetch origin
对比阶段（可选但专业）：我落后了吗？
看看云端（origin/main）比你本地（main）多了什么：
git log main..origin/main --oneline
如果没有输出，说明没人改过代码，你可以直接跳到第 4 步。

git merge origin/main   （merge之前本地也要先commit）
情况 A（顺滑）：如果没有冲突，Git 会自动合并，你的代码文件会瞬间变新。
情况 B（冲突）：如果你们改了同一行，Git 会提示 CONFLICT。你需要打开 Eclipse 改好后，再 add 和 commit。

这时：通常不需要单独打开同事的文件，因为 Git 已经贴心地把“两个版本”都塞进你的同一个文件里了，需要单独修改了

git pull 等于 fetch + merge，但很多老手坚持先 fetch

fetch后 看看云端改了啥：
git diff main origin/main


git pull origin main

git pull 的底层逻辑就是一个简单的组合公式：$$git\ pull = git\ fetch + git\ merge$$🛠️ 分解 pull 执行的两个阶段当你敲下 git pull 时，Git 在后台其实帮你跑了两趟：第一阶段：联网 Fetch (下载)Git 会连接远程服务器（GitHub），把云端最新的所有代码和版本记录下载到你的本地仓库，并存放在一个叫 origin/main 的镜像分支里。特点：这一步是纯下载，它不会碰你正在写的代码文件。第二阶段：本地 Merge (合并)下载完成后，Git 会立刻尝试把你刚刚下载的 origin/main（镜像）合入你当前所在的 main（工作区）。特点：这一步是动真格的。Git 会对比两个版本：如果没有冲突，它会自动把新代码合进去。如果有冲突，它会停下来让你手动解决。🔍 为什么老手喜欢分开操作？虽然 pull 一步到位很方便，但有些场景下分开做更专业：安全检查：如果你只想知道同事改了什么，但还不想把他的代码合进来（怕弄乱你现在的思路），你只能用 fetch。断网办公：如果你在高铁上没有网络，但你出发前执行过 fetch，你依然可以在没网的情况下执行 merge，因为数据已经都在你本地仓库的镜像里了。避免错误的合并：如果你不小心在错误的分支上执行了 pull，它会立刻把远程代码合进来，有时候会搞出一堆麻烦。